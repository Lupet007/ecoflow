# Copy this file to .env and adjust if needed. Local dev works with no .env
# at all (defaults to http://localhost:8080). When deploying, set VITE_API_URL
# as an environment variable in the hosting provider's dashboard instead of
# committing a real value here.
VITE_API_URL=http://localhost:8080
